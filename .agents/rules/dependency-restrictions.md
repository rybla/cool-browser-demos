---
trigger: always_on
glob:
description: Restrictions of which dependencies can be used.
---

You may _only_ use dependencies that are already listed under the "dependencies" property in `package.json`.
