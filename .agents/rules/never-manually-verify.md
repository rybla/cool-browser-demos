---
trigger: always_on
glob:
description: NEVER manually verify web app behavior by using the browser tools. NEVER use browser the browser tools.
---

NEVER manually verify web app behavior by using the browser tools.
NEVER use browser the browser tools.

Let the user verify the web app behavior themselves. They will give you feedback.
